package com.userservice.entity;

/**
 * Enumeration representing the roles available in the system.
 * <p>
 * Roles are used for defining access levels and permissions
 * for authenticated users.
 */
public enum RoleType {
    ADMIN, USER
}
